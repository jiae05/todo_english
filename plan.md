# TODO ENGLISH 학습체험관 페이지 — 구현 계획서 (최종 v4)

## 목적
어린이 영어 학습 앱 **TODO ENGLISH (by enuma)**의 인터랙티브 체험관 웹페이지.
학부모가 앱 구매 전에 주요 기능을 직접 눌러보고 체험할 수 있도록 하며, 무료체험 신청과 구매를 유도한다.

## 참고 레퍼런스
- milkt.co.kr/About/EduExp_IntroGate (게이트 페이지 스타일)
- milkt.co.kr/about/pc_exp (순차 가이드 투어 스타일)

---

## 산출 파일
| 파일 | 설명 |
|------|------|
| `index.html` | 체험관 페이지 (CSS·JS 모두 인라인, 단일 파일) |
| `plan.md` | 이 문서 |
| `0.jpg` ~ `6_2.jpg` | 앱 스크린샷 (index.html과 같은 폴더) |

---

## 페이지 구조 (최종)

```
① Gate Page    — 파란 배경 + 기울어진 태블릿 + 입장 버튼 + CTA
② Tour Modal   — 7단계 순차 가이드 투어 (전체화면 모달)
```

> 기능 소개 섹션(feat-grid), 인터랙티브 미니게임, Final CTA 섹션은 제거됨

---

## ① Gate Page

### 배경
- 파란 그라데이션: `linear-gradient(160deg, #0055CC, #00AAFF, #00CCEE)`
- 컬러 기하학적 도형 9개 (`.bg-s`) — 배경 장식
- 플로팅 이모지 마스코트: 🐝 🐻‍❄️ 🐱 🐶 ⭐ 🌟 (float 애니메이션)

### 레이아웃 (세로 중앙 정렬)
```
헤드라인
기울어진 태블릿 (1.jpg 표시 + 입장 버튼)
하단 CTA 카드 (구매하기 + 무료체험 신청)
```

### 태블릿 프레임
- CSS `perspective(1400px) rotateX(10deg)` 3D 기울기
- 화면 크기: `620×418px` (PC), `300×202px` (모바일)
- 화면에 `1.jpg` (홈 대시보드) 표시
- 태블릿 중앙에 주황색 `학습체험관 입장 ›` 버튼 오버레이
- 클릭 시 `openTour()` → 투어 모달 열림

### 이미지 경로
- 경로: `1.jpg` (상대경로, `./` 없이)
- 이미지 파일: `index.html`과 같은 폴더에 위치

### 하단 CTA 카드
- 흰색 반투명 카드 (`rgba(255,255,255,.94)`)
- `지금 구매하기` (주황) + `7일 무료체험 신청` (파란) 버튼
- 모든 버튼 `href="#"` 플레이스홀더 (`<!-- TODO: 실제 URL로 교체 -->`)

---

## ② Tour Modal (7단계 순차 가이드 투어)

### 구조
- `position:fixed; inset:0; z-index:1000` 전체화면 어두운 배경 (`#0a0a18`)
- 상단 바: 진행 점(●) 7개 + 단계 라벨(`1 / 7`) + `SKIP ›` 버튼
- 중앙: 태블릿 프레임 (`820×556px` PC, `320×216px` 모바일)
- 투어 완료 후: 완료 화면 + CTA 버튼

### 투어 스텝 (7단계)

| 단계 | 이미지 | 스팟라이트 | 내용 |
|------|--------|-----------|------|
| 1 | 1.jpg | 없음 | 체험관 환영 소개 |
| 2 | 1.jpg | 데일리코스 타일 `{x:16,y:38,w:36,h:47}` | 데일리 코스 설명 |
| 3 | 2.jpg | 없음 | 데일리 코스 화면 |
| 4 | 4_2.jpg | 없음 | 단어 게임 설명 |
| 5 | 3.jpg | 없음 | 오늘의 미션 설명 |
| 6 | 5_2.jpg | 없음 | Weekly Magazine 설명 |
| 7 | 6_2.jpg | 없음 | 펫 챌린지 설명 |

### 스팟라이트 효과
- `box-shadow: 0 0 0 9999px rgba(0,0,0,.58)` — 해당 영역 외 어둡게
- 좌표: `{x:%, y:%, w:%, h:%}` 퍼센트 단위

### 가이드 버블
- `position:absolute; bottom:0; left:0; right:0` — 태블릿 화면 하단 가득
- 배경: 하늘색 `rgba(140,210,245,.96)` + backdrop-filter blur
- 마지막 단계: `다음 ›` → `완료 ✓` (주황 버튼)

### 완료 화면
- 축하 이모지 🎉 + 메시지
- `지금 구매하기` (주황) + `7일 무료체험 신청` (파란) 버튼

---

## 기술 스펙

| 항목 | 내용 |
|------|------|
| 파일 형식 | 단일 HTML (CSS·JS 인라인) |
| 외부 의존 | Google Fonts CDN (Nunito) |
| 이미지 경로 | `1.jpg` 형태 (상대경로, `./` 없음) |
| 오프라인 | 폰트 외 완전 오프라인 동작 |
| 반응형 | PC(1000px+) / 태블릿(768~1000px) / 모바일(~768px) |

### 색상
```
--navy:   #2D2B5A    --pink:   #FF6B9D
--gold:   #FFD700    --purple: #8B5CF6
--orange: #F97316    --blue:   #0091FF
--green:  #22C55E
```

### 폰트
`Nunito` (Google Fonts) — 둥글고 친근한 느낌

---

## CTA 연결 처리

모든 버튼은 `href="#"` 플레이스홀더.
실제 URL 연결 시 아래 주석 검색 후 교체:
```html
<!-- TODO: 실제 URL로 교체 -->
```

---

## 검증 체크리스트

- [ ] `index.html` 브라우저(Chrome)에서 직접 열기 (file:// 프로토콜)
- [ ] 게이트 페이지: 파란 배경, 기하도형, 이모지 마스코트 float 확인
- [ ] 태블릿에 `1.jpg` 앱 화면 표시 확인
- [ ] 태블릿 3D 기울기 (perspective rotateX) 확인
- [ ] `학습체험관 입장 ›` 클릭 → 투어 모달 열림
- [ ] 투어 진행: 다음 버튼 → 이미지 fade 전환 + 가이드 텍스트 변경
- [ ] 단계 2: 데일리코스 타일 스팟라이트 하이라이트
- [ ] SKIP 클릭 → 투어 닫힘
- [ ] 마지막 단계 완료 → 완료 화면 표시
- [ ] 반응형: 모바일 768px 뷰포트
