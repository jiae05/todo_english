# TODO ENGLISH 학습체험관 페이지 — 구현 계획서 (최종 v5)

## 목적
어린이 영어 학습 앱 **TODO ENGLISH (by enuma)**의 인터랙티브 학습체험관 웹페이지.
학부모가 앱 구매 전 주요 기능을 직접 체험하고, 무료체험 신청·구매를 유도하는 단일 HTML 페이지.

## 참고 레퍼런스
- milkt.co.kr/About/EduExp_IntroGate (게이트 페이지 스타일)
- milkt.co.kr/about/pc_exp (순차 가이드 투어 스타일)

---

## 산출 파일
| 파일 | 설명 |
|------|------|
| `index.html` | 체험관 페이지 (CSS·JS 모두 인라인, 단일 파일) |
| `plan.md` | 이 문서 |
| `CLAUDE.md` | Claude Code 작업 가이드 |
| `0.jpg` ~ `6_2.jpg` | 앱 스크린샷 (index.html과 같은 폴더) |

---

## 페이지 구조 (최종)

```
① Gate Page    — 파스텔 블루 배경 + 기울어진 태블릿(0.jpg) + 하단 CTA 카드
② Tour Modal   — 10단계 순차 가이드 투어 (전체화면 모달)
```

---

## ① Gate Page

### 배경
- 파스텔 블루: `#DCE8F5` (TODO ENGLISH 공식 페이지 톤 참고)
- 기하학적 도형 9개 (`.bg-s`) — opacity `.18` 으로 매우 연하게
- 플로팅 이모지 마스코트 없음 (제거됨)

### 레이아웃
```
헤드라인 (네이비 텍스트, 강조는 핑크)
기울어진 태블릿 (0.jpg 표시 + 투명 START 버튼 오버레이)
하단 CTA 카드 (구매하기 + 무료체험 신청)
```

### 태블릿 프레임
- CSS `perspective(1400px) rotateX(10deg)` 3D 기울기
- 화면 크기: `620×418px` (PC), `300×202px` (모바일)
- 화면에 `0.jpg` (스플래시) 표시
- 입장 버튼: 투명 클릭 영역 — `0.jpg` 내 START 버튼 위에 겹침
  - `position:absolute; bottom:22%; left:50%; width:38%; height:10%`
  - 클릭 시 `openTour()` 호출

### 하단 CTA 카드
- 흰색 반투명 카드 (`rgba(255,255,255,.94)`)
- `지금 구매하기` (주황) + `7일 무료체험 신청` (파란) 버튼
- 모든 버튼: `<!-- TODO: 실제 URL로 교체 -->`

---

## ② Tour Modal (10단계 순차 가이드 투어)

### 구조
- `position:fixed; inset:0; z-index:1000` 전체화면 어두운 배경 (`#0a0a18`)
- 상단: 진행 점(●) 10개 + 단계 라벨(`1 / 10`) + `SKIP ›` 버튼
- 중앙: 태블릿 프레임 (`820×556px` PC / `320×216px` 모바일)
- 완료 후: 완료 화면 + CTA 버튼

### 투어 스텝 (10단계, 파일명 순서)

| 단계 | 이미지 | 내용 |
|------|--------|------|
| 1 | 0.jpg | 스플래시 — 체험관 환영 소개 |
| 2 | 1.jpg | 홈 대시보드 |
| 3 | 2.jpg | 데일리 코스 |
| 4 | 3.jpg | 오늘의 미션 |
| 5 | 4_1.jpg | 단어장 Glossary |
| 6 | 4_2.jpg | 단어 게임 |
| 7 | 5_1.jpg | Weekly Magazine 목록 |
| 8 | 5_2.jpg | 매거진 펼침 |
| 9 | 6_1.jpg | 펫 챌린지 방 |
| 10 | 6_2.jpg | 챌린지 맵 |

### 스팟라이트 효과
- `box-shadow: 0 0 0 9999px rgba(0,0,0,.58)` — 해당 영역 외 어둡게
- 현재 단계 2(1.jpg)에만 적용: `{x:16, y:38, w:36, h:47}`

### 가이드 버블
- 태블릿 화면 하단 고정 (`position:absolute; bottom:0; left:0; right:0`)
- 배경: 하늘색 `rgba(140,210,245,.96)` + backdrop-filter blur
- 마지막 단계(10): `다음 ›` → `완료 ✓` (주황 버튼)

---

## 기술 스펙

| 항목 | 내용 |
|------|------|
| 파일 형식 | 단일 HTML (CSS·JS 인라인) |
| 외부 의존 | Google Fonts CDN (Nunito) |
| 이미지 경로 | `0.jpg` 형태 (상대경로, `./` 없음) |
| 반응형 | PC(1000px+) / 모바일(~768px) |

### 색상
```
--navy:   #2D2B5A    --pink:   #FF6B9D
--gold:   #FFD700    --purple: #8B5CF6
--orange: #F97316    --blue:   #0091FF
--green:  #22C55E
배경: #DCE8F5
```

### 폰트
`Nunito` (Google Fonts)

---

## CTA 연결 처리

```html
<!-- TODO: 실제 URL로 교체 -->
```

---

## 검증 체크리스트

- [ ] Chrome에서 `index.html` 직접 열기 (file:// 프로토콜)
- [ ] 게이트 페이지: 파스텔 블루 배경, 기울어진 태블릿에 `0.jpg` 표시
- [ ] `0.jpg`의 START 버튼 클릭 → 투어 모달 열림
- [ ] 투어: 10단계 순서대로 이미지 전환 확인 (0.jpg → 6_2.jpg)
- [ ] 가이드 버블 텍스트 각 단계별 확인
- [ ] 마지막 단계(10) → 완료 화면
- [ ] SKIP → 투어 닫힘
- [ ] 반응형: 모바일 768px 뷰포트
